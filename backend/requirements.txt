# GPU RF Forensics Engine - Python Dependencies
# Note: RAPIDS packages (cupy, cuml, cusignal) should be installed via conda

# Core dependencies
numpy>=1.24.0
scipy>=1.11.0

# Web framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
websockets>=12.0
pydantic>=2.0.0
python-multipart>=0.0.6
httpx>=0.26.0
aiofiles>=23.2.0

# Socket.IO (for Devolution(x) Spectrum Detect frontend)
python-socketio>=5.10.0
aiohttp>=3.9.0

# Configuration
pyyaml>=6.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-benchmark>=4.0.0
pytest-cov>=4.1.0

# Development
ipython>=8.0.0
jupyterlab>=4.0.0
